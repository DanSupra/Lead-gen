name: "Automation placeholder: workflow removed"

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "The previous automation workflow was broken and has been replaced with this placeholder."
          echo "If you want me to re-add automated PR creation, tell me and I'll provide a validated workflow and instructions."
